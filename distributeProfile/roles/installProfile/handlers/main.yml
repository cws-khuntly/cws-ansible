#======  ANSIBLE  ============================================================
#          NAME:  handlers/main.yml
#   DESCRIPTION:  Handles installation of dotfiles after sync
#==============================================================================
---
- name: install
  command: "{{ remote_home }}/.dotfiles/bin/install"
